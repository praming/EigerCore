# 工作台（Workbench）前端设计稿

> 状态：仅前端设计阶段。后端（抓取/解析/调度）待用户明确说「开始后端」后再做。

## 1. 布局决策
- **形态：单页卡片仪表盘**（非左侧导航）。9 个模块以卡片网格平铺一页，支持拖拽排序、隐藏卡片、响应式列数（桌面 4 列 / 平板 2 列 / 手机 1 列）。
- **整合方式**：作为现有导航应用顶部新增「工作台」视图/板块，复用 Vue3 + Vite + TS + Pinia 与 `input.css` 设计系统。
- **范围**：本轮仅前端（静态 UI + 占位数据）。

## 2. 模块清单（9）
| 模块 | 卡片内容 | 说明 |
|---|---|---|
| 时间 | 时钟 + 日期星期 | 可设世界时 |
| 日历 | 月视图，标记待办/招标截止 | 与待办联动 |
| 待办事项 | 今日待办增删改、完成态 | 数据存贮待定 |
| 记事本 | 轻量文本/Markdown 草稿 | 本地草稿 |
| 招标信息 | 配置面板 + 命中列表 | 核心模块，见 §3 |
| 快捷书签 | 复用导航数据钉常用站 | 衔接导航应用 |
| 天气/资讯 | 天气 + 可订阅资讯流 | 后续接 API |
| 股票·基金 | 自选快照 | 后续接金融连接器 |
| 实用工具箱 | 番茄钟 / 计算器 / 单位换算 | 纯前端 |

## 3. 招标信息模块（重点）
- **左 · 抓取配置**：源列表（每行 URL + 关键词标签 + 删除）+ 添加源表单（URL 输入 + 逗号分隔关键词 + 添加源按钮）。
- **右 · 命中文章**：标题关键词高亮、来源·时间、摘要、查看原文 ↗；支持按源 / 关键词 / 时间筛选排序。
- **后端对接点（待确认后做）**：抓取源与关键词 CRUD、定时抓取调度、正文解析与关键词匹配、去重与已读状态。前端不写死任何站点，所有源/关键词均可配置。

## 4. 卡片通用交互
- 每张卡片：标题 + 折叠/隐藏；拖拽手柄重排；可配置显隐（布局偏好可持久化）。
- 视觉：白底、0.5px 边框、圆角（radius-lg）、无阴影，契合 `input.css` 设计系统。

## 5. 下一步
1. **前端骨架**（待 go-ahead）：路由/入口 + 各模块占位组件 + 招标信息静态 UI，仍不碰后端。
2. **后端构建**（待明确「开始后端」）：抓取源/关键词模型与接口、定时任务、解析匹配、去重。
